$schema: https://azuremlschemas.azureedge.net/latest/pipelineJob.schema.json
type: pipeline

display_name: used_car_analysis_model
experiment_name: random_forest_training_pipeline
description: used_car_analysis_model

inputs:
  input_data_uri:
    type: uri_file

settings:
  default_datastore: azureml:workspaceblobstore
  default_compute: azureml:cpu-cluster1
  continue_on_step_failure: false

jobs:

  ########################################
  # 1. Data preparation (prep.py)
  ########################################
  prep_data:
    type: command
    display_name: Data preparation for training
    code: ../../../data-science/src          # folder containing prep.py
    environment: azureml:used_car_analysis@latest
    command: >-
      python prep.py
      --data ${{inputs.input_data_uri}}
      --train_data ${{outputs.train_data}}
      --test_data ${{outputs.test_data}}
      --test_train_ratio ${{inputs.test_train_ratio}}
    inputs:
      data:
        type: uri_file
        path: ${{parent.inputs.input_data_uri}}
      # scalar param, no type object here
      test_train_ratio: 0.2
    outputs:
      train_data:
        type: uri_folder
      test_data:
        type: uri_folder

  ########################################
  # 2. Hyperparameter sweep (train.yml)
  ########################################
  sweep_step:
    type: sweep
    display_name: sweep_job
    sampling_algorithm: random

    # Inputs to the trial component (train.yml)
    inputs:
      train_data: ${{parent.jobs.prep_data.outputs.train_data}}
      test_data: ${{parent.jobs.prep_data.outputs.test_data}}

    outputs:
      model_output:
        type: mlflow_model

    # Command component defining the training job
    trial: ./train.yml

    search_space:
      n_estimators:
        type: choice
        values: [10, 20, 30, 50]
      max_depth:
        type: choice
        values: [3, 5, 10, 15]

    objective:
      goal: minimize
      primary_metric: MSE

    limits:
      max_total_trials: 20
      max_concurrent_trials: 10
      timeout: 7200
